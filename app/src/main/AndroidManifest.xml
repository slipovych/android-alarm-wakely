<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wakely">
        <service
            android:name=".services.AlarmService"
            android:exported="false" />

        <activity
            android:name=".screens.main.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.alarm.AlarmActivity"
            android:screenOrientation="portrait"/>

        <receiver
            android:name=".receivers.BootBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity android:name=".qrscanner.screens.QrCreatingActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".screens.alarm.AlarmRedactor"
            android:screenOrientation="portrait"/>
        <activity android:name=".qrscanner.screens.QrActivity"
            android:screenOrientation="portrait"/>

    </application>

</manifest>
